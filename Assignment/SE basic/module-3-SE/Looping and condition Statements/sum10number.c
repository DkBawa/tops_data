// 15.Calculate sum of 10 numbers using of while loop //
#include<stdio.h>
int main (){
	int i=10,n,n1,num,sum=0;
	printf("Enter a number :",num);
    scanf("%d",&num);
    printf("Enter a number :",num);
    scanf("%d",&num);

    while (i <= num){
    	sum = num + num;
       i++;
       	printf("sum of total no is :%",sum);
	}

	
	return 0;
} 
