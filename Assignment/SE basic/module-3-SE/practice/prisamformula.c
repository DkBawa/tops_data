// .find the area of a rectangular prism formula : A=2(wl+hl+hw) 
#include<stdio.h>
main () {
	int h,w,l,area ;
	printf("Enter prism height :",h);
	scanf("%d",&h);
		printf("Enter prism width :",w);
		scanf("%d",&w);
			printf("Enter prism length :",l);
			scanf("%d",&l);
			area = 2*(w*l + h*l + h*w); 
			printf("%d",area);
	
	

	
	
	
}
