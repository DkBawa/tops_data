// array ascdening order 
#include<stdio.h>
main (){
	
	int i,j,temp,a[4];

	
	for (i=0;i<4;i++){
		printf("\n Enter Elemenmt : ");
		scanf("%d",&a[i]);

	} for (i=0;i<4;i++) {
		

	for (j=i+1;j<4;j++) {
	
	}   if (a[i] > a[j]) {
		
			temp = a[i];
	        a[i] =a[j];
	        a[j] = temp;
	}  for (lagana he )
}printf("the ascending order is : %d",a[i]);
}	
	}
